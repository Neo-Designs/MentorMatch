@*
    _ViewStart.cshtml
    -----------------
    Runs before every view in this folder and its subfolders.
    Primary job: pick the default layout so individual views don't need
    to set it themselves. Any view can still override by assigning a
    different Layout (or null) in its own @{ ... } block.
*@

@{
    Layout = "_Layout";
}
